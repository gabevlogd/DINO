public class Enumerators
{
    public enum PlayerState
    {
        Run,
        Jump
    }

    public enum ObstaclesType
    {
        Cactus,
        Bird
    }

    public enum Event
    {
        UpdateScore,
        ResetMatch
    }
}
